{
    "name": "@digitalcreation/aws-lambda-actors",
    "version": "0.6.0",
    "license": "MIT",
    "devDependencies": {
        "tslint": "^6.1.3",
        "typescript": "^4.1.5"
    },
    "files": ["dist", "src"],
    "scripts":{
        "build": "npx tsc -p .",
        "push": "yarn run build && npm publish . --scope digitalcreation",
        "package": "yarn run build && npm pack ."
    }
}
