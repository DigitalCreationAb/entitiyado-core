{
    "name": "@entitiyado/core",
    "version": "0.15.0",
    "license": "MIT",
    "devDependencies": {
        "rimraf": "^3.0.2",
        "rollup": "^2.39.1",
        "rollup-plugin-dts": "^2.0.1",
        "tslint": "^6.1.3",
        "typescript": "^4.1.5"
    },
    "files": [
        "index.js",
        "index.d.ts"
    ],
    "scripts": {
        "rimraf": "./node_modules/rimraf/bin.js",
        "build": "tsc && rollup -c",
        "clean": "yarn rimraf dist && yarn rimraf node_modules && yarn rimraf index.js && yarn rimraf index.d.ts"
    }
}
